[project]
name = "dummy-project"
version = "0.1.0"
description = "A minimal dummy Python project scaffold."
authors = [
  { name = "Aslan" }
]
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "pytest>=8.0"
]

[tool.pytest]
addopts = "-q"
